{
  "name": "joyco-scripts",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "changeset": "changeset",
    "version:package": "changeset version",
    "build:cli": "pnpm -C cli build",
    "test:cli": "pnpm -C cli test",
    "release": "pnpm build:cli && changeset publish"
  },
  "workspaces": [
    "cli"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.29.8"
  }
}
